---
import Search from './Search';
import { Image } from 'astro:assets';
import testImage from './dmitri-sirobokov.jpg';
const articles = await Astro.glob('../content/articles/**/*.{md,mdx}');

const authors = await Astro.glob('../content/authors/**/*.{md,mdx}')
---

<Search client:load articles={articles} authors={authors} />
